# 验收清单（ICY 自测）

> Mac 上 **Clean Build（⇧⌘K）→ 删旧 App → 重装** 再逐项过。
> 勾 `[x]` 通过；不过就在该行后写「✗ + 现象」。🔴=重点。最后更新 2026-06-27。
> 已通过条目随时移除，仅保留「本批已修复待复验」与「尚未验收」两类。

---

## 🔧 本批已修复 · 待你复验

### K. 明信片朝向随上传照片 + 尺寸
- [ ] 上传**横图** → **横版**明信片；上传**竖图** → **竖版**；无图默认横版
- [x] 预览卡限宽（横 ≤345 / 竖 ≤300）居中，整页不变形
- 改动：`PostcardFlipCard` 与导出图统一用「明显竖图才竖版」口径。

### L. 明信片导出/分享图 = 照片 + 背面信息
- [ ] 横图导出为横版（照片在上、背面在下）；竖图导出为竖版
- [x] 免费态带 Lumi 水印；Plus 去水印高清
- 改动：新增 `PostcardExportCard`（正面板 + 背面板上下拼），`rerender` 改用它；正/背面板抽成共用组件。

---

## ⏳ 尚未验收（之前未勾选，留着一起过）

### 明信片
- [x] 「寄给」下方出现**往来联系人**快选 chips（收/发过的人）；点一下填入
- [ ] 明信片墙顶部「往来的人」头像横条；收到的卡按样式呈现 「✗ + 这个目前还没有两个机器测」

### Lumi Plus / Paywall
- [ ] paywall 三档价格（月/年/终身）显示；购买后明信片去水印高清；杀进程重开仍 Plus；恢复购买可用「✗ + 订阅之后，没有订阅中状态，分享明信片还要再点击一下订阅，才能同步订阅状态」

### 徽章
- [x] **多枚同时解锁**：只弹一个汇总弹窗「点亮了 N 个徽章」，多枚拼在一起，**不显示单个名字**；单枚仍为大图+名称+分享
- [x] 徽章插画语言：中→中文版、英→英文版、阿→阿语版；缺某语言回退底图（不空白）

## 🆕 本批新增（06-28）待验收
- [x] **成就页**：统计汇总（X/193 UN + 国家/城市/大洲 + 大洲征服）在**上**，徽章墙在**下**
- [x] **成就报告分享**：统计区右上「分享」→ 报告图（去过 N 国 + 世界地图高亮 + UN/大洲/城市 + 落款）→ 预览 + 分享/保存；三语
- [ ] **国家下钻**：全屏地图点已点亮国家 →「看看在这里去过的城市」→ 列已去城市（名+日期+次数）+ 灰显没去过的 「✗ + 功能都有了。但是想整合一点，点击了国家，再一个半浮窗/弹窗内展示：点亮国家按钮、心愿单按钮、去过城市查看入口、推荐城市列表-可以一键心愿/点亮」
- [x] **徽章文案统一**：详情/庆祝/置顶显示的名字 == 徽章图上印的字（中/英/阿；如 环球旅人 / 初见曙光 / 七大洲全集）
- [ ] **多图自动翻页**：3 张（含竖图）按序 1→2→3 自动翻，不跳页、不卡两页之间 「✗ + 还是跳页」
- [ ] **Sign in with Apple**：设置页「账户」可登录（需先在 Xcode 开能力）；登录后显示姓名 + 退出登录 「✗ + 稍后开通开发者付费后测试」
- [x] 🔴 **订阅 3 档**：scheme 设 StoreKit Configuration=Lumi.storekit 后，paywall 显示月/年/终身（见 BUILD-CONFIG）
- [x] **邮票**：land 邮票图样为**火车**（不是卡车）

---

## 🔧 本批已修复 · 待复验（06-28 第二轮）

- [ ] **订阅状态同步**：订阅成功后立即变 Plus；分享明信片页**打开即自动对齐权益**，无需再点订阅
  - 改：`PlusStore.purchase` 兜底信任刚验签的交易；`refreshEntitlement` 抽出 `isEntitling`；`PostcardSheet` 打开时 `await refreshEntitlement()`
- [ ] **国家操作整合**：全屏地图点国家 → **一个半浮窗**含：我去过这里（年/月+多城）、加入心愿、看去过的城市入口、**推荐城市列表（一键心愿 / 一键点亮）**
  - 改：`RealMapScreen` 用 `CountryActionSheet`（自带 @Query 即时刷新）取代原 confirmationDialog + 多个 sheet
- [ ] **多图自动翻页**：3 张（含竖图）按序 1→2→3，不再跳页
  - 改：`FootprintDetailView` 加「代」标记 `flipGen` + 本地 page 计数，杜绝双计时器 / 回读旧值导致的 +2 跳页

---

## 🆕 新版本提示 + 本次更新 + 触觉（06-28 第三轮）待验收

- [ ] **A 更新提示**：把本机 `MARKETING_VERSION` 临时调到低于线上版本 → 启动弹「发现新版本 X.Y」→「去更新」跳 App Store；「跳过此版本」后该版本不再弹；「稍后」下次仍弹；24h 内不重复；飞行模式/未上架 → 不弹不崩
- [ ] **B 本次更新**：全新安装（删 App 重装）→ **不弹**（播种）；改 `lumi.lastWhatsNewVersion` 为旧值或升 `MARKETING_VERSION` 重开 → 弹一次「本次更新」，再开不弹；设置页「查看本次更新」可随时再看
- [ ] **C 设置页关于**：三语「当前版本/已是最新版本/有新版本可更新/关于/查看本次更新」文案正确
- [ ] **D 触觉**：切 Tab、点亮足迹/国家、徽章庆祝、收明信片有震动（真机；系统关「触感反馈」则无）

---

## 🆕 Instagram 分享 + 个性化展示台（06-28 第四轮）待验收

- [ ] **分享到 Instagram**：装了 IG 时，明信片分享 / 成就报告 / 徽章详情出现「分享到 Instagram」→ 点按把图作为快拍背景拉起 IG Stories；未装 IG 则该按钮不显示（回退系统分享面板）
- [ ] **个性化展示台**：设置页「个性化 → 小组件 & 样式」打开展示台：①小组件轮播（点亮战绩 / 去过的国旗 / 去年今日 + 添加指引）②明信片样式 ③邮票 ④护照风格 画廊；点一下即选中（描边 + ✓）
- [ ] **样式即应用**：在展示台选的明信片样式 / 邮票 → 下次自己分享明信片时默认即用该样式（收到的卡仍保留寄件人样式）；护照风格切换即时生效
- [ ] 三语下展示台标题 / 描述 / 「分享到 Instagram」文案正确

---

### 阻塞问题记录
| # | 模块 | 现象 | 复现 |
|---|------|------|------|
| 1 |  |  |  |
| 2 |  |  |  |
